"""dldemo - teaching helpers for Deep Learning (Demo), E1234.

Submodules are disclosed as the term reaches them, so DO NOT import them eagerly here: a
partial release (say, `uncertainty` but not yet `serving`) must still import cleanly.

    from dldemo import uncertainty
"""

__version__ = "0.4.0"
__all__ = ["uncertainty", "audit", "serving"]
