"""mlfdemo - teaching helpers for Foundations of Machine Learning (Demo), E1234.

Submodules are disclosed as the term progresses, so DO NOT import them eagerly here: a
partial release (say, `datasets` but not yet `plotting`) must still import cleanly.

    from mlfdemo import datasets, linreg
"""

__version__ = "0.3.0"
__all__ = ["datasets", "linreg", "plotting"]
